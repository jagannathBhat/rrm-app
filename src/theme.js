import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
	centerizer: {
		alignItems: 'center',
		display: 'flex',
		flexDirection: 'row',
		height: '100%',
		justifyContent: 'center',
		textAlign: 'center',
		width: '100%'
	},
	root: {
		borderRadius: '0px'
	}
})
